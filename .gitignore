node_modules
.env

# Datos de runtime - se modifican en producción, no deben sobrescribirse en deploy
# devoluciones.json se genera en runtime desde devoluciones.seed.json (la semilla)
data/devoluciones.json

# Los archivos seed SÍ deben rastrearse (NO añadir *.seed.json aquí)
